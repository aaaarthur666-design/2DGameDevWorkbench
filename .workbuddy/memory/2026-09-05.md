# 2026-09-05

- 首次以 Agent 身份通读 2D Game Dev Workbench：README、manifest.json、package.json、AGENTS.md。
- 验证运行时：`npm run workbench -- list/doctor` 四能力全部 registered + configured；MCP 工具本会话未挂载，走 CLI 后备。
- 确认 work/tasks 164 条、outputs 712 文件；今晨 07:30 一批 agent-acceptance 测试任务（interactable-editor completed、sprite-generator 停 prepared 属诊断 fixture）。
- 建立项目记忆 MEMORY.md（项目定位 / 能力矩阵 / 接入方式 / 验证纪律 / 规模快照）。
- 未配 Key：GEMINI_API_KEY、OPENAI_API_KEY → map-stitcher generate-layer 会停在 awaiting_configuration。

## MCP 直连巡检（15:49）

- 写了 `tmp/mcp-inspect.mjs`：最小 MCP stdio 客户端（initialize → notifications/initialized → tools/call），可复用于会话未挂载 MCP 工具时直连 `scripts/workbench-mcp.mjs`。
- `workbench_get_environment`：SpritePipeline **online=true 但 compatible=false**（"服务在线，但接口描述不可用；请重启服务加载最新接口"），PixelLab keyConfigured=true；Node v22.22.2 + Python 已装。修复：重启本地服务（npm run dev 或 sprite-pipeline 脚本）。
- `workbench_list_presets`：2 角色（diagnostic_dummy 诊断用、player_cyber 赛博战士）× 11 动作（idle/walk/run/jump/attack/attack_in_air/melee_attack/forward_thrust/backward_evade/hurt/death）。
- `workbench_list_tasks`：今晨任务均为 agent-acceptance 验收（含 409 未审批导出、fixture 不支持 recover 等**预期失败用例**）；native jobs 2 个 fixture job（001 exported、002 review_required）。
- 用户级 MCP 挂载：`~/.workbuddy/mcp.json` 新增 `2d-game-workbench`（stdio / node / 绝对路径脚本），与 lighthouse-ops 并存；server 路径解析基于模块位置（runtime.mjs 的 repositoryRoot），不依赖 cwd。待用户在连接器管理页 Trust 后生效。

## 接口修复确认（16:02）
- 用户重启本地服务后再次 `workbench_get_environment`：`pipeline.compatible=true`，`missingRoutes=[]`，keyConfigured=true；提示语变更为「服务已就绪；Key 配置状态不代表已验证余额」。
- 结论：上次 15:49 巡检的 `compatible=false` 已彻底修复，可发起真实 PixelLab 生成。

## sprite-generator 端到端：player_cyber attack（16:04）
- 任务链：`sprite-generator-20260905080532-fvk8`（prepare）→ `...-wjka`（run, create-and-generate）→ `...-qft1`（approve）。
- 输入：characterId=player_cyber, actionId=attack, provider=pixellab, candidateCount=1, wait=false。
- 远端作业 ID：`20260905_player_cyber_attack_001`（保留可重入）。
- 预设护栏：`attack.json` frame_count=16、fps=18、loop=false、grounded=true、critical_frame_index=6；`identity_description`=「cyber warrior identity, blue-black armour, cyan highlights, body proportions, helmet, sword silhouette, and right-facing side view」——**无披风元素**，QA palette_distance + area_change_ratio 兜底检测。
- 用户特别提示「不要凭空出现披风」→ 风险由 preset identity_description 消除（无需手动补 negative prompt），目检作为最后一道关。
- 真实 PixelLab 生成耗时 ~2.5 分钟；轮询间隔 45-90s 合理，schema 默认 `wait=false` 避免阻塞。
- 目检关键帧：frame-000 起始 / frame-003 蓄力 / frame-006 接触关键帧 / frame-008 命中 / frame-016 回收，对照 idle_reference.png 全部一致，**未观察到披风或其他凭空服装/武器元素**，approve 通过（remoteStatus=approved）。
- 产物：`outputs/sprite-generator-20260905080536-wjka/frames/candidate-01/frame-000..016.png` 共 17 张。**根因已查清（16:14 用户追问后核对 result.json）**：请求 16 帧（prompt 明确 exactly 16），PixelLab animate-with-text-v3 实际返回 17 帧，服务策略 `preserve_all_returned` 全部保留不裁剪；frame-015/016 sha256 不同非重复，016 是 provider 多渲染的收招 settle 帧。严格 16 帧需在 action 预设配 `provider_frame_selection`（仅影响未来作业）。
- **配额现状**：本单耗 4 generations，trial 订阅 12/40 → **剩 8，只够再生成 2 个动作**；「剩余 10 动作全量生成」需先充值/升级。
- 经验：approve 操作独立 taskId（`...-qft1`），reviewNote 必填且要写明「实际查看图像后的判断依据」，引用具体帧号 + 预设名 + 参考图路径。

## 后续可选项（等用户决策）
- 跑 `export`（同 operation 集合）：输出 sprite-sheet.png + preview.gif，落地 `outputs/...-wjka/`。
- 增量动作：player_cyber + run/walk/jump/hurt/death 等剩余 10 个动作。
- 如果要护栏生成时，加 image2image/negative prompt 通道，需要先扩 manifest 与 workflow。
