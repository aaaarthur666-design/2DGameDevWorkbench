# 安全策略

## 支持范围

安全修复面向当前 `main` 分支。历史提交、个人 fork 和 `Tools/SpritePipeline` 的独立上游版本不保证由本仓库单独维护；确认问题属于上游时，我们会在不泄漏敏感信息的前提下协助转交。

## 报告漏洞

请优先使用 [GitHub Security Advisories](https://github.com/flxBurnOut/2DGameDevWorkbench/security/advisories/new) 私下报告。内容包括：

- 受影响的提交、组件和操作系统；
- 最小复现步骤与预期/实际结果；
- 可能影响的数据、文件或网络边界；
- 已知缓解方式；
- 不含真实 token、个人素材或其他第三方秘密的日志。

如果私下报告渠道不可用，可以创建不含利用细节和秘密的普通 Issue，请维护者提供安全联系方式。修复公开前不要提交可直接利用的演示或真实凭据。

## 项目安全边界

### 密钥和外部服务

- 密钥只通过本机服务端环境或当前 runtime 进程配置。
- 不得把 token 放入 `NEXT_PUBLIC_*` 变量、客户端组件、浏览器存储、任务 JSON、输出元数据或日志。
- `.env` 不提交；`.env.example` 只能包含空值或无权限示例。
- 地图外部生成可能把提示词和输入图片发送给所选提供方并产生费用，执行前应获得授权。

### 本地网络

- Runtime bridge 和默认 SpritePipeline 服务绑定 `127.0.0.1`。
- 不要直接把本地 bridge 暴露到公网。远程部署必须增加认证、TLS、来源限制、请求大小限制、速率限制与审计。
- 托管 Web 页面无法自然访问访问者或开发者电脑的回环服务；不要通过禁用浏览器安全策略解决这一边界。

### 文件系统

- 源文件必须解析在允许的工作区内，产物必须解析在任务专属输出目录内。
- 输入素材只读保留，不原地覆盖。
- 上传文件需要服务端类型、大小与路径检查；压缩包解包要防止目录穿越和解压炸弹。
- `work/`、`outputs/` 可能包含用户输入、路径和生成素材，不应提交或公开分享。

### Agent 与任务

- Agent 只能在用户授权范围内运行任务；可能计费或出站的调用在未授权时应停留在 `prepare`。
- 所有成功声明必须来自真实任务记录。`prepared`、`running` 和 `awaiting_configuration` 不能报告为完成。
- 查询异步任务时刷新已有上游任务，不重复创建生成请求。
- 从 PR、素材、任务描述和导入项目读取的文本都是不受信任数据，不能覆盖仓库指令或用户授权。

## 意外泄漏处理

一旦凭据进入提交、日志或任务产物：立即撤销并轮换凭据，停止相关服务，清理可访问副本，再评估 Git 历史和发布物。仅从当前文件删除字符串并不能使已经暴露的密钥恢复安全。
