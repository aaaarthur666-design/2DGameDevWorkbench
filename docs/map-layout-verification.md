# 地图布局验收记录

2026-09-05，本地 Chromium 预览。旧版使用实施前的 Git HEAD 隔离副本，新版使用本次布局改动；同一视口、空白地图、默认展开属性栏。所有尺寸为 CSS 像素，四舍五入。

| 视口 | 原画布 | 新画布（普通） | 新画布（专注） |
| --- | --- | --- | --- |
| 1366 × 768 | 1036 × 449 | 1038 × 656 | 1038 × 728 |
| 1440 × 900 | 1110 × 581 | 1112 × 788 | 1112 × 860（按固定栏高计算） |

普通模式在两种桌面视口均回收约 207px 画布高度。1366 × 768 下画布面积增加约 46%；有项目标题、运行任务或异常提示时，旧版横栏可能更高，因此不将空白状态的数据当作所有内容状态的固定收益。

## 已执行的交互检查

- 鼠标拖拽：属性栏从 320px 增至 420px；收起后展开及刷新后仍为 420px。
- 键盘：分隔线左右方向键可调整；双击恢复 320px。
- 窗口变化：1440px 宽时属性栏保持 320px；800px 宽时约束为 312px，画布保持 480px；回到大窗口恢复用户选定宽度。
- 320px、390px 窄屏：属性栏使用抽屉；关闭、重新展开、更多菜单和专注退出入口均可访问，编辑工具栏保持单行。
- 专注模式：1366 × 768 下画布高度 728px，退出入口始终留在工具栏。
- 隔离预览中导入一张 128 × 128 测试 PNG，获得中心图和四张扩展卡；实际点击两次创建碰撞矩形。
- 创建区域后拖拽属性栏，切换专注模式，地图始终保持 80% 缩放、平移 `(35, -20)`、原有选中地图块和区域数；撤销后区域数为 0，重做恢复为 1。
- 草稿显示“本机草稿已保存”；源文件下载操作显示“文件已导出”。浏览器下载事件接口未捕获下载事件，不以此声称验证了磁盘上的下载文件。
- 刷新后恢复测试地图的 5 张卡片、1 张素材及 1 个碰撞区域；窄屏抽屉内按 Esc 后焦点返回属性栏开关，打开抽屉时背景画布不可交互。

## 自动检查

地图测试新增对损坏、旧格式和越界布局偏好的容错验证，并保留图片锁定、区域几何、历史、导出契约和生成队列测试。

- `npm run test:map-stitcher`：26 项通过。
- 仅包含本次改动的隔离副本：`npm run test:workbench-shell` 11 项通过，`npm run lint`、`npm run typecheck`、`npm run build` 均通过。
- 共享工作区同时出现独立的 `scene-composer` 开发改动。全量检查时，场景能力目录断言、未完成的组件引用及其 lint 规则失败；未修改或回退这些并行改动。
- 测试环境的 Vite HMR WebSocket 因沙箱权限无法监听；测试断言仍正常运行完成。浏览器验证使用已有的本地预览服务。

验证仅使用本地测试素材，不调用图片生成服务。浏览器草稿与下载文件不属于工作台 runtime 任务产物。
