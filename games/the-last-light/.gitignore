# Godot 4 editor cache / import artifacts
.godot/

# Export presets contain local signing paths; add back selectively at WP-11 if needed
export_presets.cfg

# Export builds (WP-11 deliverables go here; decide VCS strategy per plan §15.1 before committing binaries)
/export/

# OS noise
.DS_Store
Thumbs.db
